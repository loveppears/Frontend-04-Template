<template>
    <div>
        <img/>
    </div>
</template>

<script>
export default {
    el: '#example',
    data: {
        message: 'hello'
    },
    computed: {
        // a computed getter
        reversedMessage: function() {
            // `this` points to the vm instance
            let i = 1;
            let s = "<html>"
            while(i < 100) {
                i++;
            }
            return this.message.split('').reverse().join(')
        }
    }
}
<script>